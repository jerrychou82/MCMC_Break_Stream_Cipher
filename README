REAME file for MCMC_Break_Stream_Cipher

1. Compile
> make

2. Execution
> ./mcmc_stream_cipher [textfilename] [#iteration] [#cycle]

3. Default settings
    (a) Key length
        The default setting of key length is in stream_cipher.h

